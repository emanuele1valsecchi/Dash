{
  "name": "dash-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security-rule tests for firestore.rules, run against the local Firestore emulator. No test account and no real Firebase project are involved.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project dash-rules-test \"node --test\"",
    "emulator": "firebase emulators:start --only firestore --project dash-rules-test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "14.19.0"
  }
}
